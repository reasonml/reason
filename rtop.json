{
  "name": "@esy-ocaml/rtop",
  "version": "3.8.1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/reasonml/reason.git"
  },
  "dependencies": {
    "@esy-ocaml/substs": "^0.0.1",
    "@opam/ocamlfind": "*",
    "@opam/result": "*",
    "@opam/dune": "2.9.3",
    "@opam/reason": "^3.8.0",
    "@opam/utop": " >= 1.17.0",
    "ocaml": ">= 4.2.0 < 5.1.0"
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "ocaml": "~4.12.0"
  },
  "esy": {
    "build": [
      [
        "dune",
        "build",
        "-p",
        "rtop"
      ]
    ]
  }
}
