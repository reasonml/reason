/* Keyword operators in parentheses should not be escaped with \# */
let (mod) = mod_float;
let (land) = (land);
let (lor) = (lor);
let (lsl) = (lsl);
let (lsr) = (lsr);
let (lxor) = (lxor);
let (asr) = (asr);
let (or) = (||);
