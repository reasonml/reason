(jbuild_version 1)

(install
 ((section share)
  (package reason-rtop)
  (files (rtop_init.ml))))

(library
 ((name rtop)
  (public_name reason-rtop)
  (modules (reason_util reason_utop reason_toploop))
  (wrapped false)
  (libraries (reason utop))))